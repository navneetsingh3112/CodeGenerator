package in.novopay.network.requests;

import android.content.Context;

import java.io.Serializable;

/**
 * Created by ${entity.author} on ${entity.date}.
 */

public class Delete${entity.upperCamelCaseClassName}Request extends GenericRequest implements Serializable{
    private Request request = new Request();

    public Delete${entity.upperCamelCaseClassName}Request(Context context) {
        setStaticHeaders(context);
    }

    public Request getRequest() {
        return request;
    }

    public void setRequest(Request request) {
        this.request = request;
    }

    public class Request{
		private String id;
   		
		public String getId() {
            return id;
        }

        public void setId(String id) {
            this.id = id;
        }
    }
}