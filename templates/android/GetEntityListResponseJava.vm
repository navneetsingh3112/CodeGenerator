package in.novopay.network.responses;

import java.io.Serializable;
import java.util.ArrayList;

/**
 * Created by ${entity.author} on ${entity.date}.
 */

public class Get${entity.upperCamelCaseClassName}ListResponse extends GenericResponse implements Serializable{
	private ArrayList<Get${entity.upperCamelCaseClassName}Details> get${entity.upperCamelCaseClassName}List;
	private String pageSize;
    private String offset;
    private String numberOfRecords;

    public ArrayList<Get${entity.upperCamelCaseClassName}Details> get${entity.upperCamelCaseClassName}List() {
        return get${entity.upperCamelCaseClassName}List;
    }

    public void set${entity.upperCamelCaseClassName}List(ArrayList<Get${entity.upperCamelCaseClassName}Details> get${entity.upperCamelCaseClassName}List) {
        this.get${entity.upperCamelCaseClassName}List = get${entity.upperCamelCaseClassName}List;
    }
    
     public String getPageSize() {
        return pageSize;
    }

    public void setPageSize(String pageSize) {
        this.pageSize = pageSize;
    }

    public String getOffset() {
        return offset;
    }

    public void setOffset(String offset) {
        this.offset = offset;
    }

    public String getNumberOfRecords() {
        return numberOfRecords;
    }

    public void setNumberOfRecords(String numberOfRecords) {
        this.numberOfRecords = numberOfRecords;
    }
	
	public class Get${entity.upperCamelCaseClassName}Details{ 
		private String id;
	#foreach ($field in $entity.fieldList)
	#if( ${field.lowerCamelCaseName} != "createdOn" && ${field.lowerCamelCaseName} != "createdBy" && ${field.lowerCamelCaseName} != "updatedOn" && ${field.lowerCamelCaseName} != "updatedBy")
		private ${field.javaType} ${field.lowerCamelCaseName};
	#end	
	#end
	
		public String getId() {
			return id;
		}
	
		public void setId(String id) {
	    	this.id = id;
	    }
	    
	#foreach ($field in $entity.fieldList)
	#if( ${field.lowerCamelCaseName} != "createdOn" && ${field.lowerCamelCaseName} != "createdBy" && ${field.lowerCamelCaseName} != "updatedOn" && ${field.lowerCamelCaseName} != "updatedBy")
		public ${field.javaType} get${field.upperCamelCaseName}(){
			return ${field.lowerCamelCaseName};
		}
		
		public void set${field.upperCamelCaseName}(${field.javaType} ${field.lowerCamelCaseName}){
			this.${field.lowerCamelCaseName} = ${field.lowerCamelCaseName};
		}
		
	#end
	#end
	}
}